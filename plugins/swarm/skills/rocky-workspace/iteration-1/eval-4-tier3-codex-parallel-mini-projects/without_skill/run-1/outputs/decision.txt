MECHANISM: Spawn 3 Codex agents in parallel via the mcp__codex__codex MCP tool, one per mini-project (calculator, file_utils, string_utils). Each Codex call gets its own cwd (projects/calculator, projects/file_utils, projects/string_utils), workspace-write sandbox, and a self-contained prompt specifying module name, required functions, and a TDD requirement (pytest tests + 100% coverage on the small surface). All 3 mcp__codex__codex calls go in a SINGLE assistant message so they execute in parallel. For tmux visibility: before dispatching, create the 3 project directories, then open a tmux session with 3 vertically-split panes each tailing that project's log file (e.g. `tail -F projects/<name>/codex.log`); the Codex prompts are instructed to append progress lines to that log so the user sees live activity in each pane. (Caveat: mcp__codex__codex runs in-process inside Claude Code, so the panes show log tails, not literal Codex TUIs — true Codex-TUI-per-pane would require shelling out `codex exec` per pane via tmux send-keys instead, which is the alternative if the user wants raw Codex output.)

COORDINATION: None needed at runtime — the 3 projects are fully independent (separate dirs, separate files, no shared imports), which is the textbook case for superpowers:dispatching-parallel-agents. No task list, no message bus, no shared state. I wait for all 3 Codex calls to return, then aggregate results. If one fails I re-dispatch only that one with the error context; the other two are unaffected.

REVIEW: After all 3 return, for each project I (a) Read the module + tests, (b) run `pytest --cov=<module> --cov-report=term-missing` to confirm green + coverage on the small surfaces (target 100% per the financial/business rule for tiny utility modules), (c) sanity-check against Karpathy guidelines (no speculative abstractions, ≤50-line functions, type hints, immutability for string/file ops), (d) one quick simplify pass if any module is bloated. No cross-project review needed since they don't interact.

REASONING: 3 truly independent small tasks → parallel dispatch is the right shape (dispatching-parallel-agents skill applies). Codex MCP is the cheapest way to offload mechanical TDD-shaped work without burning my own context on boilerplate. Tmux panes tail per-project logs because mcp__codex__codex is in-process; if Luke wants literal Codex TUIs in each pane, switch to `tmux send-keys` driving `codex exec` subprocesses instead.

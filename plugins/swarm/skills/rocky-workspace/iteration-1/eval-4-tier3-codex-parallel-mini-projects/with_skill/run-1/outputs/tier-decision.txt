TIER: 3
ANNOUNCEMENT: Tier 3, Codex Parallel via multiple Codex panes (3 coders), Codex plan reviewer, Claude code reviewer.
SIGNALS WALKED:
  Tier 0: doesn't fit — 3 separate mini-projects across multiple files/directories, not ≤20 LOC, not single-file mechanical work, requires design judgment per module.
  Tier 1: doesn't fit — exceeds 1–3 files (3 modules × multiple files each), user explicitly wants visible parallel execution in tmux panes, not a single in-process one-shot dispatch.
  Tier 2: doesn't fit — user explicitly asks for 3 agents working in parallel ("have 3 codex agents... in parallel"), which is the opposite of Tier 2's sequential single-coder SDD shape; throughput from parallelism is the explicit goal.
  Tier 3: fits — (1) "3+ tasks that are TRULY independent (disjoint files/modules)" satisfied: calculator / file utilities / string utilities each in its own directory under projects/, fully independent files; (2) user explicitly wants visible multi-pane orchestration ("i want to watch them work in tmux panes"); (3) tie-breaker "user says 'spawn agents' / 'I want to watch it' → bump to Tier 3" matches "have 3 codex agents" + "watch them work in tmux panes".
DECISION A: Codex — no --agents flag passed, default coder identity for Tier 3 is Codex (per Decision A table: "default → Codex"). User also explicitly said "codex agents", reinforcing the default.
DECISION B: Multiple Codex panes via swarm_spawn_agent in wave layout (Phase 3.3 wave dispatch, bash transport via swarm-transport.sh). Per Decision B table row "Tier 3 / Codex (default)": "Multiple Codex panes via swarm_spawn_agent in wave layout (existing Phase 3.3 wave dispatch). Bash transport." NOT Native AgentTeam (that's the Tier 3 + --agents claude variant), NOT mcp__codex__codex (HARD RULE forbids MCP for dispatch).
CROSS-MODEL: Plan reviewer = Codex pane (Phase 2.5.2 spawns it — Codex pane is always the plan reviewer per the cross-model rule recap, regardless of coder identity). Code reviewer = Claude Opus in-process via Skill("superpowers:requesting-code-review") (Phase 4.3b) — because the coder is Codex, the opposite-model code reviewer is Claude Opus per the rule "Codex coder → Claude reviews via Skill('superpowers:requesting-code-review') in Opus's session — Phase 4.3b".

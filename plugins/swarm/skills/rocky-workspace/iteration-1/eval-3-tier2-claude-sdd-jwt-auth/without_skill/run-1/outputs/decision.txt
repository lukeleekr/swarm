MECHANISM: Inline implementation in this same Opus session using TDD per the user's testing rules. Sequence: (1) brainstorm + clarify scope, (2) write a short plan with phases (model, hashing, JWT, endpoints, middleware, tests), (3) RED/GREEN/REFACTOR per endpoint using pytest, (4) self-review against coding-and-patterns.md and karpathy.md before finishing. No subagents, no Codex panes — user explicitly said "use claude only" and "keep it inside the same opus session".
COORDINATION: N/A — single-agent execution. Would use TaskCreate/TaskUpdate to track the phases (User model, password hashing, JWT utils, 5 endpoints, middleware, tests) so progress is visible, but all work happens in this session.
REVIEW: Self-review by the same Opus session at the end — re-read diffs against karpathy guidelines (surgical changes, no speculative abstraction), coding-and-patterns.md (Pydantic at boundaries, AppError hierarchy, no hardcoded secrets, env-loaded JWT secret), and run the full pytest suite to confirm 100% coverage on the auth path (auth is in the "100% coverage" bucket per workflow-and-testing.md). No second model invoked because the user excluded Codex and didn't ask for a separate reviewer agent.
REASONING: User gave two hard constraints — Claude only, same Opus session — which rules out Codex panes, swarm dispatch, and spawning subagents. The task is ~6-8 files of well-trodden FastAPI/JWT territory, comfortably within one Opus session with TDD, so inline execution is the right fit and matches the karpathy "no speculative complexity" rule.
